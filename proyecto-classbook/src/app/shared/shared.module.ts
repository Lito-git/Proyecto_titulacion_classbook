// Con la arquitectura standalone los componentes compartidos
// se importan directamente en cada componente que los necesite
// por lo que este módulo queda vacío y solo se mantiene por compatibilidad
import { NgModule } from '@angular/core';

@NgModule({})
export class SharedModule { }